/// Legacy placeholder.
///
/// The production mobile app now authenticates directly against the website
/// backend and does not require Firebase runtime initialization.
class DefaultFirebaseOptions {
  const DefaultFirebaseOptions._();
}
